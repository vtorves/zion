
#DEMANDAS GERAIS / FECHADAS
<div class="table-responsive">
      <table class="table table-bordered">
        <thead>
          <tr>
            <th>#id</th>
            <th>STATUS</th>
            <th>GRUPO</th>
            <th>CONTA PAI</th>
            <th>ASSUNTO</th>
            <th>LOCAL</th>
            <th>DETALHES</th>
          </tr>
        </thead>
        <tbody>
        <% @tickets.each do |ticket| %>
          <tr>
            <td><%= ticket.id %></td>
            <td><%= ticket.status %></td>
            <td><%= ticket.group.name %></td>
            <td><%= ticket.account.lastname %></td>
            <td><%= ticket.subject %></td>
            <td><%= ticket.local %></td>
            <td><%= ticket.id %></td>
          </tr>
    <% end %>

         </tbody>
      </table>
    </div>

<%= @tickets.inspect %>